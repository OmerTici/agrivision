import SwiftUI

enum CarniColors {
    static let purple = Color(red: 104 / 255, green: 79 / 255, blue: 163 / 255)
    static let purpleDark = Color(red: 82 / 255, green: 58 / 255, blue: 138 / 255)
    static let purpleLight = Color(red: 130 / 255, green: 108 / 255, blue: 190 / 255)
    static let white = Color.white
    static let cardOverlay = Color.white.opacity(0.14)
    static let fieldBackground = Color.white
    static let mutedText = Color.white.opacity(0.78)

    static let appBackground = Color(red: 247 / 255, green: 245 / 255, blue: 250 / 255)
    static let tabBar = Color.white
    static let tabInactive = Color(red: 150 / 255, green: 150 / 255, blue: 160 / 255)
    static let tabSelectedTint = Color(red: 104 / 255, green: 79 / 255, blue: 163 / 255).opacity(0.12)
}
